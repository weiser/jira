#!/usr/bin/env ruby

require 'jira'
require 'optparse'

opts = OptionParser.new do |opts|
  opts.banner = "Jira example usage: jira [command] [args]"

  Jira.registered_commands.each do |command|
    opts.banner += "\n\t#{command.description}"
  end
end

puts opts




